import { ReplaceBreaksToBrPipe } from './replace-breaks-to-br.pipe';

describe('ReplaceBreaksToBrPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceBreaksToBrPipe();
    expect(pipe).toBeTruthy();
  });
});
