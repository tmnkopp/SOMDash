import { SnippetFormaterPipe } from './snippet-formater.pipe';

describe('SnippetFormaterPipe', () => {
  it('create an instance', () => {
    const pipe = new SnippetFormaterPipe();
    expect(pipe).toBeTruthy();
  });
});
